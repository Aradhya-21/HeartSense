{
  "buildCommand": "cd frontend && npm install && npm run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/backend.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
